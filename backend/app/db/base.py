from sqlalchemy.orm import declarative_base

# Declarative base for SQLAlchemy models
Base = declarative_base()

# Note: Do not create the complete application schema yet.
# Models will be defined in the app.models package in future phases.
